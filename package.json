{
  "name": "claude-key-proxy",
  "version": "1.0.0",
  "description": "Claude API Key load balancer — auto-rotate, smart failover, real-time dashboard",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "keywords": ["claude", "anthropic", "api", "proxy", "load-balancer", "key-rotation"],
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  }
}
